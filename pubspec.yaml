name: phone_shop
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"  # ⬅️ IMPORTANT: Must be 3.0.0 or higher

dependencies:
  flutter:
    sdk: flutter

  # State Management
  provider: ^6.1.2

  # Database
  sqflite: ^2.3.3+2
  sqflite_common_ffi: ^2.3.3+1
  path: ^1.9.0
  path_provider: ^2.1.4

  # UI & Utilities
  intl: ^0.19.0
  shared_preferences: ^2.3.2
  image_picker: ^1.1.2
  flutter_screenutil: ^5.9.3

  # Charts
  fl_chart: ^0.69.0

  # Barcode
 

  # Icons
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # assets:
  #   - assets/images/flutter